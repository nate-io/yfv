{
  "name": "yfv-landing",
  "version": "1.0.0",
  "description": "Landing page for YFV LLC",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "mkdir -p dist && cp -r public/* dist/",
    "deploy": "npm run build && firebase deploy --only hosting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "author": "",
  "license": "ISC"
}
